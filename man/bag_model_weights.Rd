% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bag_models.R
\name{bag_model_weights}
\alias{bag_model_weights}
\alias{w_strech_maxmin_squared}
\alias{w_strech_max_squared}
\alias{score2weight_mean}
\alias{score2weight_min_mean}
\alias{score2weight_invmean}
\alias{score2weight_min_invmean}
\title{Helper functions for weight calculation in bag models}
\usage{
w_strech_maxmin_squared(x)

w_strech_max_squared(x)

score2weight_mean(x, col = "validation_score", score_threshold = 0.7)

score2weight_min_mean(x, col = "validation_score", score_threshold = 0.7)

score2weight_invmean(x, col = "validation_score", score_threshold = 0.7)

score2weight_min_invmean(x, col = "validation_score", score_threshold = 0.7)
}
\arguments{
\item{x}{\verb{[list or numeric]} \cr For \verb{score2weight_*} functions: a model result list with
a column specified by \code{col} parameter. For \verb{w_strech_*} functions: a numeric vector of weights.}

\item{col}{\verb{[character(1)="validation_score"]} \cr Column name in \code{x} containing validation scores.}

\item{score_threshold}{\verb{[numeric(1)=0.7]} \cr Minimum validation score threshold.
Models with any validation score below this are assigned zero weight.}
}
\value{
Numeric vector or value representing normalized weights or summary statistics.
}
\description{
These functions compute weights for ensemble model combinations based on validation scores.
They handle score thresholding and weight normalization differently:
\itemize{
\item \verb{score2weight_*} functions convert validation scores to weights
\item \verb{w_strech_*} functions normalize weights after they are computed
}
}
\details{
\strong{Score to weight conversion functions:}
\itemize{
\item \code{score2weight_mean}: Uses mean of all validation scores
\item \code{score2weight_min_mean}: Uses mean only if all scores above threshold
\item \code{score2weight_invmean}: Uses inverse of mean (harmonic mean-related)
\item \code{score2weight_min_invmean}: Uses inverse mean only if all scores above threshold
}

\strong{Weight stretching/normalization functions:}
\itemize{
\item \code{w_strech_maxmin_squared}: Rescales to \link{0,1}, squares, then normalizes to sum to 1
\item \code{w_strech_max_squared}: Divides by max, squares, then normalizes to sum to 1
}
}
\seealso{
\code{\link[=bag_models]{bag_models()}}, \code{\link[=data_summary]{data_summary()}}
}
\keyword{internal}
