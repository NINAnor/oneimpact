# Find inflection points in a curve

Helper function to find inflection points in a curve, using the response
axis values.

## Usage

``` r
inflection(x)
```

## Arguments

- \`x\`:

  `[vector,numeric]`  
  Vector of `y` values, i.e., the response variable of a curve.
